# =============================================================================
# 本地开发环境变量（仅用于本地 npm run dev 开发）
# =============================================================================
# 注意：
#  1. VITE_ 前缀使变量在浏览器中可访问
#  2. 切勿提交真实密钥到 Git（.env 已在 .gitignore 中）
#  3. v0.2.3 后移除了自建 OSS 配置，使用百炼临时存储

# =============================================================================
# v0.2.2 新增：Supabase 配置（账号体系 - 必须配置）
# =============================================================================
# 获取地址: https://supabase.com/dashboard/project/_/settings/api
# 说明：用于用户认证和数据存储，必须在部署前配置

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# =============================================================================
# AI 模型 API Key（可选 - 用于开发测试）
# =============================================================================
# 说明：这些 Key 可在页面设置中配置，也可在此配置用于本地开发测试
# 阿里百炼: https://dashscope.console.aliyun.com/apiKey
# 火山方舟: https://console.volcengine.com/ark/region:ark+cn-beijing/apiKey

# 阿里百炼 API Key（可选）
# v0.2.3 后：百炼临时存储也需要此 Key 进行文件上传
VITE_DASHSCOPE_API_KEY=your_dashscope_api_key

# 火山引擎 ARK API Key（可选）
VITE_ARK_API_KEY=your_ark_api_key

# =============================================================================
# 已移除的环境变量（v0.2.3）
# =============================================================================
# 以下环境变量在 v0.2.3 中已移除，使用百炼临时存储替代自建 OSS：
#
# VITE_ALIYUN_ACCESS_KEY_ID      - 不再需要
# VITE_ALIYUN_ACCESS_KEY_SECRET  - 不再需要
# VITE_ALIYUN_OSS_REGION         - 不再需要
# VITE_ALIYUN_OSS_BUCKET         - 不再需要
# ALIYUN_ACCESS_KEY_ID           - 不再需要
# ALIYUN_ACCESS_KEY_SECRET       - 不再需要
# ALIYUN_ROLE_ARN                - 不再需要（移除了 STS Serverless Function）
# ALIYUN_OSS_REGION              - 不再需要
# ALIYUN_OSS_BUCKET              - 不再需要
